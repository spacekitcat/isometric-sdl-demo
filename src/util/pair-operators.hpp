#pragma once

#include <iostream>

class PairOperators {
public:
  static std::pair<float, float> addPair(std::pair<float, float> first,
                                         std::pair<float, float> second);
};